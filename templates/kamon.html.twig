{% macro getUrl(clan) %}{% apply spaceless %}
{% if clan.kamonUpload is empty %}
	{{ asset(clan.kamon) }}
{% else %}
	{{ asset('kamon/' ~ clan.kamonUpload) | imagine_filter('kamon') }}
{% endif %}
{% endapply %}{% endmacro %}