{% macro getUrl(user) %}
{% if user.avatar is empty %}
	{% if user.gender == "m" %}
		{{ asset('images/boyz.jpg') }}
	{% else %}
		{{ asset('images/girlz.jpg') }}
	{% endif %}
{% else %}
	{{ asset('avatar/' ~ user.avatar) | imagine_filter('avatar') }}
{% endif %}
{% endmacro %}